# Install Sanity CLI tooling

`npm install -g @sanity/cli`

# Project

To get started with a new project, run this command after you have installed the CLI tool:

`sanity init`

# Run the Studio

`sanity start`
